import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create your account",
  description: "Start tracking spending, budgets and savings goals with an AI financial copilot. Free forever plan.",
};

export default function CreateyouraccountLayout({ children }: { children: React.ReactNode }) {
  return children;
}